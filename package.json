{
  "name": "sample-vuejs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
	"start": "serve",
	"build": "mkdir _static && cp index.html _static"
  },
  "devDependencies": {
    "serve": "^11.3.2"
  }
}
